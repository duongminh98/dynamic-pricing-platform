# GENERATED by deploy/render_k8s.py — do not edit by hand.
# Champion seed Job (post-migration: register_models.py against pricing_db)
apiVersion: batch/v1
kind: Job
metadata:
  name: seed-champions
  namespace: dpp
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: pricing-runtime-sa-ksa
      initContainers:
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
        restartPolicy: Always
        args: ["--private-ip", "--port=5432", "dpp-staging-260703:asia-southeast1:dpp-pg"]
        resources:
          requests: {cpu: 50m, memory: 64Mi}
      containers:
      - name: seed
        image: asia-southeast1-docker.pkg.dev/dpp-staging-260703/dpp/lifecycle:latest
        command: ["sh", "-c"]
        args:
          - "python -m app.bootstrap_reference_data && python offline/register_models.py"
        env:
        - name: POSTGRES_PASSWORD
          valueFrom: {secretKeyRef: {name: dpp-secrets, key: db-password}}
        - name: POSTGRES_USER
          value: "platform_user"
        - name: PRICING_DB_HOST
          value: "127.0.0.1"
        - name: PRICING_DB_PORT
          value: "5432"
        - name: PRICING_DB_NAME
          value: "pricing_db"
        - name: OBJECT_STORAGE_PROVIDER
          value: "gcs"
        - name: REFERENCE_DATA_URI
          value: "gs://dpp-pricing-reference-staging"
        - name: PRICING_REFERENCE_DIR
          value: "/app/data/synthetic_real_1m_history_lift_v2"
        - name: PRICING_MODELS_DIR
          value: "/app/reports/modeling/models"
