# GENERATED by deploy/render_k8s.py — do not edit by hand.
# notification-service (Java, private ClusterIP + Cloud SQL proxy sidecar)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: dpp
  labels: {app: notification-service, tier: backend}
spec:
  replicas: 1
  selector: {matchLabels: {app: notification-service}}
  template:
    metadata:
      labels: {app: notification-service, tier: backend}
    spec:
      serviceAccountName: notification-service-sa
      containers:
      - name: notification-service
        image: asia-southeast1-docker.pkg.dev/dpp-staging-260703/dpp/notification-service:latest
        ports: [{containerPort: 8080}]
        env:
        - name: POSTGRES_USER
          value: "platform_user"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: db-password
        - name: SPRING_FLYWAY_ENABLED
          value: "true"
        - name: RABBITMQ_HOST
          value: "rabbitmq.dpp.svc.cluster.local"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "platform_user"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: rabbitmq-password
        - name: NOTIFICATION_DB_HOST
          value: "127.0.0.1"
        - name: NOTIFICATION_DB_PORT
          value: "5432"
        - name: NOTIFICATION_DB_NAME
          value: "notification_db"
        - name: MAIL_HOST
          value: "mailpit.dpp.svc.cluster.local"
        - name: MAIL_PORT
          value: "1025"
        - name: MAIL_FROM
          value: "noreply@dpp-pricing.dev"
        - name: NOTIFICATION_EMAIL_ENABLED
          value: "true"
        readinessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 60
          periodSeconds: 20
        resources:
          requests: {cpu: 250m, memory: 512Mi}
          limits: {memory: 1Gi}
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
        args:
          - "--private-ip"
          - "--structured-logs"
          - "--port=5432"
          - "dpp-staging-260703:asia-southeast1:dpp-pg"
        securityContext:
          runAsNonRoot: true
        resources:
          requests: {cpu: 50m, memory: 64Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: dpp
spec:
  type: ClusterIP
  selector: {app: notification-service}
  ports: [{port: 8080, targetPort: 8080}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service
  namespace: dpp
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: notification-service}
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource: {name: cpu, target: {type: Utilization, averageUtilization: 70}}
