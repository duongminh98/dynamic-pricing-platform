# GENERATED by deploy/render_k8s.py — do not edit by hand.
# billing-service (Java, private ClusterIP + Cloud SQL proxy sidecar)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: dpp
  labels: {app: billing-service, tier: backend}
spec:
  replicas: 1
  selector: {matchLabels: {app: billing-service}}
  template:
    metadata:
      labels: {app: billing-service, tier: backend}
    spec:
      serviceAccountName: billing-service-sa
      containers:
      - name: billing-service
        image: asia-southeast1-docker.pkg.dev/dpp-staging-260703/dpp/billing-service:latest
        ports: [{containerPort: 8080}]
        env:
        - name: POSTGRES_USER
          value: "platform_user"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: db-password
        - name: SPRING_FLYWAY_ENABLED
          value: "true"
        - name: RABBITMQ_HOST
          value: "rabbitmq.dpp.svc.cluster.local"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "platform_user"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: rabbitmq-password
        - name: BILLING_DB_HOST
          value: "127.0.0.1"
        - name: BILLING_DB_PORT
          value: "5432"
        - name: BILLING_DB_NAME
          value: "billing_db"
        - name: VNP_TMN_CODE
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: vnp-tmn-code
        - name: VNP_HASH_SECRET
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: vnp-hash-secret
        - name: VNP_PAY_URL
          value: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
        - name: VNP_API_URL
          value: "https://sandbox.vnpayment.vn/merchant_webapi/api/transaction"
        - name: VNP_RETURN_URL
          value: "https://app.dpp-pricing.dev/payment-result"
        startupProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 30
        readinessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 60
          periodSeconds: 20
        resources:
          requests: {cpu: 250m, memory: 512Mi}
          limits: {memory: 1Gi}
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
        args:
          - "--private-ip"
          - "--structured-logs"
          - "--port=5432"
          - "dpp-staging-260703:asia-southeast1:dpp-pg"
        securityContext:
          runAsNonRoot: true
        resources:
          requests: {cpu: 50m, memory: 64Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: dpp
spec:
  type: ClusterIP
  selector: {app: billing-service}
  ports: [{port: 8080, targetPort: 8080}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: billing-service
  namespace: dpp
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: billing-service}
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource: {name: cpu, target: {type: Utilization, averageUtilization: 70}}
