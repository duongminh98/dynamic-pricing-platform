# GENERATED by deploy/render_k8s.py — do not edit by hand.
# claims-service (Java, private ClusterIP + Cloud SQL proxy sidecar)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: claims-service
  namespace: dpp
  labels: {app: claims-service, tier: backend}
spec:
  replicas: 1
  selector: {matchLabels: {app: claims-service}}
  template:
    metadata:
      labels: {app: claims-service, tier: backend}
    spec:
      serviceAccountName: claims-service-sa
      containers:
      - name: claims-service
        image: asia-southeast1-docker.pkg.dev/dpp-staging-260703/dpp/claims-service:latest
        ports: [{containerPort: 8080}]
        env:
        - name: POSTGRES_USER
          value: "platform_user"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: db-password
        - name: SPRING_FLYWAY_ENABLED
          value: "false"
        - name: RABBITMQ_HOST
          value: "rabbitmq.dpp.svc.cluster.local"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "platform_user"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: rabbitmq-password
        - name: CLAIMS_DB_HOST
          value: "127.0.0.1"
        - name: CLAIMS_DB_PORT
          value: "5432"
        - name: CLAIMS_DB_NAME
          value: "claims_db"
        readinessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet: {path: /actuator/health, port: 8080}
          initialDelaySeconds: 60
          periodSeconds: 20
        resources:
          requests: {cpu: 250m, memory: 512Mi}
          limits: {memory: 1Gi}
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
        args:
          - "--private-ip"
          - "--structured-logs"
          - "--port=5432"
          - "dpp-staging-260703:asia-southeast1:dpp-pg"
        securityContext:
          runAsNonRoot: true
        resources:
          requests: {cpu: 50m, memory: 64Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: claims-service
  namespace: dpp
spec:
  type: ClusterIP
  selector: {app: claims-service}
  ports: [{port: 8080, targetPort: 8080}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: claims-service
  namespace: dpp
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: claims-service}
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource: {name: cpu, target: {type: Utilization, averageUtilization: 70}}
