# GENERATED by deploy/render_k8s.py — do not edit by hand.
# pricing-service (FastAPI, private ClusterIP + Cloud SQL proxy + GCS reference bootstrap)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-service
  namespace: dpp
  labels: {app: pricing-service, tier: backend}
spec:
  replicas: 3
  selector: {matchLabels: {app: pricing-service}}
  template:
    metadata:
      labels: {app: pricing-service, tier: backend}
    spec:
      serviceAccountName: pricing-runtime-sa-ksa
      containers:
      - name: pricing-service
        image: asia-southeast1-docker.pkg.dev/dpp-staging-260703/dpp/pricing-service:latest
        ports: [{containerPort: 8000}]
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: db-password
        - name: DATABASE_URL
          value: "postgresql+psycopg2://platform_user:$(POSTGRES_PASSWORD)@127.0.0.1:5432/pricing_db"
        - name: PRICING_DATABASE_URL
          value: "postgresql+psycopg2://platform_user:$(POSTGRES_PASSWORD)@127.0.0.1:5432/pricing_db"
        - name: RUN_MIGRATIONS
          value: "false"
        - name: RABBITMQ_HOST
          value: "rabbitmq.dpp.svc.cluster.local"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "platform_user"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dpp-secrets
              key: rabbitmq-password
        - name: OBJECT_STORAGE_PROVIDER
          value: "gcs"
        - name: OBJECT_STORAGE_MODEL_BUCKET
          value: "dpp-pricing-models-staging"
        - name: OBJECT_STORAGE_REPORT_BUCKET
          value: "dpp-pricing-reports-staging"
        - name: OBJECT_STORAGE_DATASET_BUCKET
          value: "dpp-pricing-datasets-staging"
        - name: REFERENCE_DATA_URI
          value: "gs://dpp-pricing-reference-staging"
        - name: PRICING_REFERENCE_DIR
          value: "/data/synthetic_real_1m_history_lift_v2"
        - name: PRICING_MODELS_DIR
          value: "/reports/modeling/models"
        - name: MODEL_ARTIFACT_CACHE_DIR
          value: "/tmp/model-cache"
        - name: QUOTE_MAX_CONCURRENCY
          value: "12"
        - name: PRICING_DB_POOL_SIZE
          value: "8"
        - name: PRICING_DB_MAX_OVERFLOW
          value: "4"
        startupProbe:
          httpGet: {path: /health, port: 8000}
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 30
        readinessProbe:
          httpGet: {path: /health, port: 8000}
          initialDelaySeconds: 40
          periodSeconds: 10
        livenessProbe:
          httpGet: {path: /health, port: 8000}
          initialDelaySeconds: 90
          periodSeconds: 20
        resources:
          requests: {cpu: 750m, memory: 1Gi}
          limits: {memory: 2Gi}
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.11.0
        args:
          - "--private-ip"
          - "--structured-logs"
          - "--port=5432"
          - "dpp-staging-260703:asia-southeast1:dpp-pg"
        securityContext:
          runAsNonRoot: true
        resources:
          requests: {cpu: 50m, memory: 64Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: pricing-service
  namespace: dpp
spec:
  type: ClusterIP
  selector: {app: pricing-service}
  ports: [{port: 8000, targetPort: 8000}]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pricing-service
  namespace: dpp
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: pricing-service}
  minReplicas: 3
  maxReplicas: 6
  metrics:
  - type: Resource
    resource: {name: cpu, target: {type: Utilization, averageUtilization: 60}}
