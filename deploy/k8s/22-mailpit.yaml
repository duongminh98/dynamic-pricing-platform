# GENERATED by deploy/render_k8s.py — do not edit by hand.
# Mailpit (dev SMTP capture; replace with real relay in prod)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpit
  namespace: dpp
spec:
  replicas: 1
  selector: {matchLabels: {app: mailpit}}
  template:
    metadata:
      labels: {app: mailpit}
    spec:
      containers:
      - name: mailpit
        image: axllent/mailpit:latest
        ports:
        - {containerPort: 1025, name: smtp}
        - {containerPort: 8025, name: ui}
        env:
        - {name: MP_SMTP_AUTH_ACCEPT_ANY, value: "true"}
        - {name: MP_SMTP_AUTH_ALLOW_INSECURE, value: "true"}
        resources:
          requests: {cpu: 25m, memory: 64Mi}
---
apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: dpp
spec:
  type: ClusterIP
  selector: {app: mailpit}
  ports:
  - {port: 1025, targetPort: 1025, name: smtp}
  - {port: 8025, targetPort: 8025, name: ui}
