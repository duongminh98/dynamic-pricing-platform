# Python common module dependencies for Pricing_Service
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
prometheus_client>=0.21.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
hypothesis>=6.100.0

# Model artifacts are pickled with scikit-learn + LightGBM; the loader
# aliases sklearn._loss._loss so joblib.load can unpickle them.
scikit-learn>=1.4.0
lightgbm>=4.0.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# SQLAlchemy ORM for model version / champion assignment / audit trail tables
SQLAlchemy>=2.0.0
